# 🎨 Настройка яркости фоновых элементов лендинга

## ✅ Внесённые изменения

Увеличена яркость всех фоновых элементов для лучшей видимости на IPS и OLED мониторах.

## 📊 Параметры для настройки гаммы

Все параметры находятся в файле: `employees/templates/employees/landing.html`

### 1. **Координатная сетка** (строки ~43-56)
```css
.schematic-layer {
    background-image: 
        /* Основная сетка */
        repeating-linear-gradient(0deg, transparent, transparent 49px, 
            rgba(0, 150, 255, 0.18) /* ← Яркость сетки */
        ...
        /* Диагональные линии */
        repeating-linear-gradient(45deg, transparent, transparent 99px, 
            rgba(0, 200, 255, 0.08) /* ← Яркость диагоналей */
```

**Параметры:**
- **Яркость основной сетки:** `0.18` (рекомендуется 0.10-0.30)
  - Было: `0.08` (слишком темно)
  - Сейчас: `0.18` (хорошо видно)
  - Если слишком яркая: уменьшите до `0.12-0.15`
  - Если слишком тусклая: увеличьте до `0.22-0.25`

- **Яркость диагоналей:** `0.08` (рекомендуется 0.03-0.15)
  - Было: `0.03` (почти не видно)
  - Сейчас: `0.08` (заметно)
  - Для более выраженного эффекта: `0.10-0.12`

---

### 2. **Тёмное наложение** (строки ~60-68)
```css
.parallax-overlay {
    background: rgba(0, 0, 0, 0.4); /* ← Затемнение фона */
}
```

**Параметры:**
- **Затемнение фона:** `0.4` (рекомендуется 0.2-0.7)
  - Было: `0.6` (слишком темно)
  - Сейчас: `0.4` (сбалансировано)
  - Для светлого фона: `0.2-0.3`
  - Для тёмного фона: `0.5-0.7`

> **Важно:** Чем меньше значение, тем ярче фоновые элементы, но хуже читаемость текста!

---

### 3. **Технические надписи (CONTROL LOOP, DATA BUS и т.д.)** (строки ~71-96)
```css
.tech-annotations {
    color: rgba(0, 200, 255, 0.65); /* ← Яркость аннотаций */
}

@keyframes fadeInOut {
    10%, 90% { opacity: 0.75; } /* ← Яркость при мерцании */
    50% { opacity: 0.6; }       /* ← Средняя яркость */
}
```

**Параметры:**
- **Базовая яркость аннотаций:** `0.65` (рекомендуется 0.4-0.9)
  - Было: `0.4` (тусклые)
  - Сейчас: `0.65` (хорошо видны)
  
- **Максимальная яркость при мерцании:** `0.75` (рекомендуется 0.5-0.9)
  - Было: `0.6`
  - Сейчас: `0.75`

- **Средняя яркость:** `0.6` (рекомендуется 0.4-0.8)
  - Было: `0.4`
  - Сейчас: `0.6`

---

### 4. **Технические символы (GOST, ISO, IEC, PID, Modbus и т.д.)** (строки ~157-172)
```css
.symbol {
    color: rgba(0, 200, 255, 0.6);              /* ← Базовая яркость */
    text-shadow: 0 0 10px rgba(0, 200, 255, 0.5); /* ← Свечение */
}

@keyframes symbolPulse {
    10%, 90% { opacity: 0.7; }  /* ← Максимальная яркость */
    50% { opacity: 0.55; }      /* ← Средняя яркость */
}
```

**Параметры:**
- **Базовая яркость символов:** `0.6` (рекомендуется 0.3-0.8)
  - Было: `0.35` (почти не видно)
  - Сейчас: `0.6` (отчётливо видны)

- **Свечение символов:** `0.5` (рекомендуется 0.3-0.7)
  - Было: `0.3` (слабое)
  - Сейчас: `0.5` (заметное)

- **Максимальная opacity при пульсации:** `0.7` (рекомендуется 0.5-0.9)
  - Было: `0.5`
  - Сейчас: `0.7`

- **Средняя opacity:** `0.55` (рекомендуется 0.4-0.7)
  - Было: `0.35`
  - Сейчас: `0.55`

---

## 🎯 Рекомендации по настройке

### Для IPS мониторов с хорошей контрастностью:
- Сетка: `0.15-0.18`
- Затемнение: `0.4-0.45`
- Аннотации: `0.60-0.70`
- Символы: `0.55-0.65`

### Для OLED мониторов (глубокий чёрный):
- Сетка: `0.18-0.22`
- Затемнение: `0.35-0.40`
- Аннотации: `0.65-0.75`
- Символы: `0.60-0.70`

### Для обычных LCD мониторов:
- Сетка: `0.12-0.15`
- Затемнение: `0.45-0.50`
- Аннотации: `0.55-0.65`
- Символы: `0.50-0.60`

### Для ярких помещений:
Увеличьте все значения на `0.05-0.10`

### Для тёмных помещений:
Уменьшите все значения на `0.05-0.10`

---

## 🔄 Как применить изменения

1. Откройте файл `employees/templates/employees/landing.html`
2. Найдите нужный параметр по комментариям `/* ПАРАМЕТР: ... */`
3. Измените значение
4. Обновите страницу в браузере (Ctrl+Shift+R для жёсткой перезагрузки)

---

## 📝 Пример настройки для вашего случая

Если на вашем мониторе элементы всё ещё слабо видны, попробуйте:

```css
/* Увеличить яркость сетки */
rgba(0, 150, 255, 0.22)  /* было 0.18 */

/* Уменьшить затемнение */
rgba(0, 0, 0, 0.35)      /* было 0.4 */

/* Увеличить яркость символов */
rgba(0, 200, 255, 0.7)   /* было 0.6 */
opacity: 0.8;            /* было 0.7 */
```

---

## ⚠️ Важно

- Значения в `rgba()` - это альфа-канал (прозрачность)
- `0.0` = полностью прозрачно (невидимо)
- `1.0` = полностью непрозрачно (максимальная яркость)
- Увеличение яркости фона может ухудшить читаемость основного текста
- Всегда тестируйте изменения на разных мониторах

---

## 🎨 Текущая конфигурация (после изменений)

| Элемент | Было | Стало | Диапазон |
|---------|------|-------|----------|
| Основная сетка | 0.08 | **0.25** ⬆️ | 0.10-0.30 |
| Диагональные линии | 0.03 | **0.12** ⬆️ | 0.03-0.15 |
| Затемнение фона | 0.6 | **0.35** ⬇️ | 0.2-0.7 |
| Базовая яркость аннотаций | 0.4 | **0.75** ⬆️ | 0.4-0.9 |
| Мерцание аннотаций | 0.6/0.4 | **0.85/0.7** ⬆️ | 0.5-0.9 |
| Базовая яркость символов | 0.35 | **0.72** ⬆️ | 0.3-0.8 |
| Свечение символов | 0.3 | **0.6** ⬆️ | 0.3-0.7 |
| Размер свечения | 8px | **12px** ⬆️ | 8-15px |
| Пульсация символов | 0.5/0.35 | **0.8/0.65** ⬆️ | 0.5-0.9 |

### 📊 Увеличение относительно оригинала:

- ✨ Сетка: **+312%** (0.08 → 0.25)
- ✨ Диагонали: **+400%** (0.03 → 0.12)
- ✨ Аннотации: **+187%** (0.4 → 0.75)
- ✨ Символы: **+206%** (0.35 → 0.72)
- 🌗 Затемнение: **-42%** (0.6 → 0.35) - фон светлее!
